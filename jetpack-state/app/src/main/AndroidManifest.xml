<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="icc.personal.jetpack.state">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackState">


        <activity
            android:name=".todo.TodoActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackState.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".examples.HelloCodelabActivity"
            android:exported="true"
            />

        <activity android:name=".examples.HelloCodeLabActivityWithViewModel"
            android:exported="true"
            />

    </application>

</manifest>